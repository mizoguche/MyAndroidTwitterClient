<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>

        <item name="android:windowBackground">@color/bg</item>
        <item name="android:textViewStyle">@style/text_view_style</item>
        <item name="android:editTextStyle">@style/edit_text_style</item>
    </style>

    <style name="text_view_style" parent="@android:style/Widget.Holo.Light.TextView">
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorLink">@color/primary</item>
    </style>

    <style name="edit_text_style" parent="@android:style/Widget.Holo.Light.EditText">
        <item name="android:textColor">@color/text</item>
    </style>

    <style name="ToolbarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@color/primary</item>
        <item name="android:textColorPrimary">@color/textOnPrimary</item>
    </style>

    <style name="ToolbarPopupTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:actionMenuTextColor">@color/text</item>
    </style>

    <style name="TransparentText" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/textOnPrimaryTransparent</item>
    </style>

    <style name="TextOnPrimary" parent="@android:style/TextAppearance.Material.Widget.Toolbar.Title">
        <item name="android:textColor">@color/textOnPrimary</item>
    </style>
</resources>
